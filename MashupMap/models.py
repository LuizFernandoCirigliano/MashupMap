class Artist(object):
    """docstring for Artist"""
    def __init__(self, artistID, name, imageURL):
        super(Artist, self).__init__()
        self.artistID = artistID
        self.name = name
        self.imageURL = imageURL

